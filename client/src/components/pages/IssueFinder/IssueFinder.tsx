import { ContentContainer } from "../../organisms/ContentContainer";
import styles from "./IssueFinder.scss";

export const IssueFinder = () => {
  return (
    <ContentContainer>
      <div className={styles.issueFinder}>IssueFinder</div>
    </ContentContainer>
  );
};
