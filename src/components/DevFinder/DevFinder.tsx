import styles from "./DevFinder.module.scss";
import { ContentContainer } from "../ContentContainer";

export const DevFinder = () => {
  return (
    <ContentContainer>
      <div className={styles.devFinder}>DevFinder</div>
    </ContentContainer>
  );
};
